const target = {
    red: 'rojo',
    green: 'verde',
    blue: 'azul'
}
const handler ={
    get(obj, prop) {
        if(prop in obj){
            return obj[prop]
        }
        const suggestion = 
    }
}